import LoginForm from 'components/LoginForm/LoginForm';
import { Division } from './Login.styled';

const Login = () => {
  return (
    <Division>
      <LoginForm />
    </Division>
  );
};
export default Login;
